import React from 'react'

const Footer = () => {
    return (<div className="todo-footer">
        <span className="count-todos">
            {/* diisi dengan props "count" */}}</span>
        {/*  buatlah sebuah ternary operation di mana apabila props count > 1 maka akan m
 *  enuliskan text "items left" dan jika props count <= 1 maka akan menuliskan "
 * i tem left"

 */
        }
    </div>

    )
}

export default Footer